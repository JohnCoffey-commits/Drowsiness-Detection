# Figure Captions

**Example Temporal Evidence Timeline.** Example saved video-upload demonstration run showing specialist evidence probabilities (`p_eye_closed` and `p_yawn`) together with warning-candidate fusion states over time. This figure is warning-candidate evidence only and is not ground-truth drowsiness annotation or a detection-accuracy result.

**Warning-candidate Interval Timeline.** Warning-candidate interval timeline from a saved video-upload demonstration run. Intervals represent temporal fusion outputs based on eye and mouth/yawn evidence, not ground-truth drowsiness labels.

**Keyframe Contact Sheet.** Representative keyframes generated by the saved analysis run for the high-confidence warning-candidate segment. These keyframes provide visual context for the runtime evidence and are not labelled ground-truth drowsiness examples.

**Live Monitor Interface Screenshot.** Interface view of the VisionGuard live-monitor route. The screenshot should be described as a product/interface view unless paired with a documented real runtime session; no warning state is fabricated.

**Video Upload Analysis Interface Screenshot.** Interface view of the video-upload analysis route. If no analysis result is loaded in the screenshot, it should be treated as a static interface state rather than evidence of runtime output.

**History 48h Interface Screenshot.** Interface view of the History 48h route using local/demo or current local archive records. The screenshot must not be described as production user history.

**Insights Interface Screenshot.** Interface view of the Insights route. Data shown should be described as local archive or fallback/local records rather than production analytics.

**Remote Demo Deployment Architecture.** Remote demonstration architecture for VisionGuard: Vercel frontend access through a Cloudflare Quick Tunnel to a local FastAPI backend with local model checkpoints and a local compact SQLite archive. This is not a production deployment; the temporary tunnel URL and running local backend are required.
